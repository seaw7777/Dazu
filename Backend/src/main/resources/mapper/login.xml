<?xml version="1.0" encoding="UTF-8"?>

<mapper namespace="com.web.dazu.mapper.LoginMapper">
	<select id="getUserInfo" parameterType="string">
		select * 
		from member
		where usercode = #{id}
	</select>
</mapper>